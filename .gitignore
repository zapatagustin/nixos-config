# nix build outputs
result
result-*

# agent tooling cache (machine-specific skill paths, regenerated per host)
.atl/

# flake lock (intentionally untracked; reproducibility trade-off accepted)
flake.lock

# local Claude Code overrides (WiFi passwords, per-machine config)
.claude/settings.local.json
.claude/settings.local.*

# Python bytecode
__pycache__/
*.pyc

# vendored opencode plugin deps (bun install, regenerated from bun.lock)
node_modules/
