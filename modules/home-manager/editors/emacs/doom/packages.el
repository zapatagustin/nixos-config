;; -*- no-byte-compile: t; -*-
;;; packages.el
;; Extra elisp packages beyond what the init.el modules pull in.
;; (package! some-package) here, then `doom sync`.
;; CLI tools/LSP servers do NOT go here — they go in default.nix.
