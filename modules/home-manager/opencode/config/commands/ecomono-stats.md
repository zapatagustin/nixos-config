---
description: Show ecomono lifetime token-savings stats
---
Show ecomono stats — total tokens saved, sessions, average compression ratio.

Read the lifetime history log at `~/.config/ecomono/.ecomono-history.jsonl`
(or wherever the ecomono-stats script writes it). Output: total saved,
sessions counted, avg ratio. One short table.
